import PagePlaceholder from "../../components/ui/PagePlaceholder";

const ClientItems = () => (
  <PagePlaceholder title="Client Items" description="Track client-owned inventory items." />
);

export default ClientItems;
